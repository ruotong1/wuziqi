# ⚡ 后端部署 - 快速开始

## 🎯 目标

部署Express后端到Railway，让前端可以调用API。

---

## 🚀 5步完成部署

### 1️⃣ 注册Railway
👉 https://railway.app → 点击 **"Login with GitHub"**

### 2️⃣ 创建项目
- 点击 **"New Project"**
- 选择 **"Deploy from GitHub repo"**
- 选择仓库：**`ruotong1/wuziqi`**

### 3️⃣ 配置项目
- **Root Directory**: `chikawa-server`
- **Start Command**: `npm start`（通常自动检测）

### 4️⃣ 添加环境变量
在 **Variables** 标签中添加：

| 变量名 | 值 | 说明 |
|--------|-----|------|
| `JWT_SECRET` | （生成随机密钥） | 见下方生成命令 |
| `JWT_REFRESH_SECRET` | （生成随机密钥） | 见下方生成命令 |
| `VOLCENGINE_SECRET_KEY` | `你的火山引擎密钥` | 从火山引擎控制台获取 |

**生成密钥命令**（在终端运行）：
```bash
node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
```
运行两次，分别用作 `JWT_SECRET` 和 `JWT_REFRESH_SECRET`。

### 5️⃣ 获取URL并测试
- Railway会自动部署
- 获取URL：`https://your-app.up.railway.app`
- 测试：访问 `https://your-app.up.railway.app/health`

---

## 🔗 连接前端

1. **Cloudflare Pages** → 项目 → **Settings** → **Environment variables**
2. 添加：`VITE_API_URL` = `https://your-app.up.railway.app`
3. 重新部署前端

---

## ✅ 完成检查

- [ ] Railway项目已创建
- [ ] 环境变量已配置
- [ ] 部署成功，获得URL
- [ ] `/health` 可以访问
- [ ] 前端已配置API地址

---

**详细说明请查看：`后端部署指南_Railway.md`**





