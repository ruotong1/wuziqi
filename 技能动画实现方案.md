# 技能动画实现方案

## 方案一：Canvas 逐帧动画（推荐 - 当前使用）

### 优势
- 支持多张独立图片，无需预处理
- 流畅的动画播放
- 易于控制播放速度和循环
- 支持动态加载图片

### 使用方法
将三张图片放在 `public` 目录下：
- `skill-anim-1.png` (第一帧)
- `skill-anim-2.png` (第二帧)  
- `skill-anim-3.png` (第三帧)

### 代码实现
已在 `GomokuGame.jsx` 中实现 Canvas 动画组件。

---

## 方案二：CSS 雪碧图逐帧动画（高级方案）

### 制作雪碧图
1. 将三张图片横向排列（或纵向排列）
2. 使用 Photoshop/GIMP 等工具合并成一张图片
3. 假设每帧宽度为 450px，总宽度 = 450px × 3 = 1350px
4. 保存为 `skill-anim-sprite.png`

### CSS 代码示例
```css
.skill-sprite-animation {
  width: 450px;
  height: 450px;
  background-image: url('/skill-anim-sprite.png');
  background-size: 1350px 450px; /* 总宽度 单帧高度 */
  background-position: 0 0;
  animation: skillSpritePlay 0.8s steps(3) forwards;
  /* steps(3) 表示分成3步，对应3帧 */
  /* forwards 表示动画结束后保持最后一帧 */
}

@keyframes skillSpritePlay {
  0% {
    background-position: 0 0; /* 第1帧 */
  }
  33.33% {
    background-position: -450px 0; /* 第2帧 */
  }
  66.66% {
    background-position: -900px 0; /* 第3帧 */
  }
  100% {
    background-position: -900px 0; /* 保持第3帧 */
  }
}
```

### 关键点
- `steps(3)`：必须设置，表示动画分成3步完成（对应3帧）
- 去掉 `infinite`：动画只播放一次
- `animation-duration`：控制播放速度（0.8s = 每帧约0.27秒）
- `background-position`：通过负值移动背景位置来显示不同帧

### 如果要使用雪碧图方案
1. 制作雪碧图（三张图片横向排列）
2. 放在 `public/skill-anim-sprite.png`
3. 替换 JSX 中的 Canvas 实现为 div + CSS 类名

