# 📊 五子棋项目结构图

## 🌳 完整项目结构

```
cursor zrt/
│
├── 📁 src/
│   ├── 📁 components/
│   │   ├── 📄 GomokuGame.jsx          ⭐ 核心游戏组件（563行）
│   │   └── 📄 GomokuGame.css          🎨 游戏样式（Super Mario风格）
│   │
│   ├── 📁 pages/
│   │   ├── 📄 Gomoku.jsx              📄 页面包装组件
│   │   └── 📄 Gomoku.css              🎨 页面样式
│   │
│   └── 📄 App.jsx                     🔀 路由配置（包含 /gomoku 路由）
│
└── 📄 package.json                    📦 项目依赖配置
```

## 🔗 组件关系

```
App.jsx
  └── Routes
      └── /gomoku → Gomoku.jsx
                      └── GomokuGame.jsx (核心逻辑)
                          ├── GomokuGame.css (样式)
                          └── LocalStorage (数据存储)
```

## 📋 文件详细说明

### 🎮 核心文件

| 文件 | 行数 | 功能 | 重要性 |
|------|------|------|--------|
| `GomokuGame.jsx` | 563行 | 游戏逻辑、AI、状态管理 | ⭐⭐⭐⭐⭐ |
| `GomokuGame.css` | ~400行 | Super Mario风格样式 | ⭐⭐⭐⭐ |
| `Gomoku.jsx` | 14行 | 页面包装组件 | ⭐⭐ |
| `Gomoku.css` | ~10行 | 页面基础样式 | ⭐ |

### 📦 依赖关系

```
GomokuGame.jsx
  ├── React (useState, useEffect, useCallback, useRef)
  ├── LocalStorage API (数据持久化)
  └── CSS (GomokuGame.css)

Gomoku.jsx
  ├── React
  ├── GomokuGame (导入)
  └── CSS (Gomoku.css)

App.jsx
  ├── React Router (BrowserRouter, Routes, Route)
  └── Gomoku (导入)
```

## 🎯 功能模块划分

```
GomokuGame.jsx
│
├── 🎮 游戏状态管理
│   ├── board (15×15棋盘)
│   ├── currentPlayer (当前玩家)
│   ├── gameOver (游戏状态)
│   └── winner (获胜方)
│
├── 🤖 AI系统
│   ├── makeAiMove() (AI落子)
│   └── evaluatePosition() (位置评估)
│
├── ✅ 游戏逻辑
│   ├── handleCellClick() (玩家落子)
│   ├── checkWin() (获胜判定)
│   └── handleReset() (重置游戏)
│
├── 📊 历史记录
│   ├── gameHistory (历史数据)
│   ├── saveGameRecord() (保存记录)
│   └── clearHistory() (清空记录)
│
└── 🎨 UI渲染
    ├── 棋盘渲染
    ├── 棋子渲染
    ├── 获胜线渲染 (SVG)
    └── 历史面板
```

## 💾 数据流

```
用户操作
  ↓
handleCellClick()
  ↓
更新 board 状态
  ↓
checkWin() 检查获胜
  ↓
如果玩家回合结束 → makeAiMove()
  ↓
AI落子 → 更新 board
  ↓
checkWin() 检查AI是否获胜
  ↓
游戏结束 → saveGameRecord()
  ↓
更新 gameHistory → LocalStorage
```

## 🎨 样式架构

```
GomokuGame.css
├── 全局样式
│   ├── 背景渐变（天空蓝）
│   └── 像素字体（Press Start 2P）
│
├── 容器样式
│   ├── .gomoku-container (米色背景)
│   └── .gomoku-game (主容器)
│
├── 棋盘样式
│   ├── .gomoku-board (棕色木纹)
│   └── .intersection (交叉点)
│
├── 棋子样式
│   ├── .stone.black (黑色方形)
│   └── .stone.white (白色方形)
│
└── UI组件
    ├── 按钮样式 (3D效果)
    ├── 历史面板
    └── 获胜线 (SVG渐变)
```

## 🔌 API/接口（当前无后端）

```
当前状态: 纯前端，无后端API

数据存储:
  └── LocalStorage
      └── key: "gomoku-history"
          └── value: JSON数组

未来扩展（如需要）:
  ├── WebSocket (实时对战)
  ├── REST API (排行榜、用户系统)
  └── 数据库 (PostgreSQL/MongoDB)
```

## 📊 代码统计

```
总文件数: 4个
  ├── JSX文件: 2个
  └── CSS文件: 2个

代码行数:
  ├── GomokuGame.jsx: 563行
  ├── GomokuGame.css: ~400行
  ├── Gomoku.jsx: 14行
  └── Gomoku.css: ~10行
  └── 总计: ~987行

依赖:
  ├── React: 核心框架
  ├── React Router: 路由
  └── 无其他外部依赖
```

## 🚀 部署架构

```
前端部署 (Cloudflare Pages)
  └── 静态文件
      ├── index.html
      ├── assets/
      └── _redirects (SPA路由支持)

后端部署: 无（当前纯前端）

数据存储: 浏览器LocalStorage
```

---

**最后更新**: 2026-01-05





