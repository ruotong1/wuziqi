# 🚀 五子棋游戏 - 快速启动说明

## ✅ 项目已构建完成

生产版本已生成在 `dist` 文件夹中。

---

## 🎯 三种启动方式

### 方式1：使用启动脚本（最简单）⭐

**Mac/Linux:**
```bash
./start.sh
```

**Windows:**
```cmd
start.bat
```

脚本会自动检测Python并启动服务器，然后在浏览器打开：
```
http://localhost:8080/gomoku
```

---

### 方式2：手动使用Python

```bash
cd dist
python3 -m http.server 8080
# 或 Python 2
python -m SimpleHTTPServer 8080
```

然后在浏览器打开：
```
http://localhost:8080/gomoku
```

---

### 方式3：使用Node.js serve

```bash
# 安装 serve（如果还没有）
npm install -g serve

# 启动服务器
cd dist
serve -s . -l 8080
```

然后在浏览器打开：
```
http://localhost:8080/gomoku
```

---

### 方式4：开发模式（推荐用于开发）

```bash
npm install
npm run dev
```

然后在浏览器打开：
```
http://localhost:5173/gomoku
```

---

## ⚠️ 重要提示

**React应用不能直接双击HTML文件运行！**

必须通过HTTP服务器访问，因为：
- React使用了模块系统（ES6 modules）
- 使用了React Router进行路由
- 浏览器安全限制

---

## 📋 快速检查清单

- [ ] 已运行 `npm run build`（或使用启动脚本自动构建）
- [ ] `dist` 文件夹已生成
- [ ] 已启动本地服务器
- [ ] 浏览器访问 `http://localhost:8080/gomoku`
- [ ] 游戏可以正常运行

---

## 🎮 开始游戏

启动服务器后：
1. 打开浏览器
2. 访问 `http://localhost:8080/gomoku`
3. 开始游戏！

---

**提示**: 使用 `start.sh` 或 `start.bat` 脚本是最简单的方式！





